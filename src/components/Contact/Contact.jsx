import './Contact.sass';
import { Section } from '../Section/Section';

const options = {
    id: 'contact',
    title: 'CONTACT'
}

export const Contact = () => {
  return <Section options={options}>
    
  </Section>;
};
